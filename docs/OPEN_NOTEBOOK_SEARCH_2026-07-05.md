# Open Notebook Search 2026-07-05

## Result

Best candidate found:

```text
LRriver/NotebookLM-Lite
https://github.com/LRriver/NotebookLM-Lite
```

The exact phrase `Open Notebook` is ambiguous. Several small repos exist, including `tom1030507/OpenNotebookLM` and `hsn07pk/open-notebook`, but the best initial path is to inspect `NotebookLM-Lite` before installing anything.

## KAIZEN7 Verdict

Use as optional provider candidate for:

```text
source-pack -> briefing -> dossier/script
```

Do not use as:

- main memory,
- Obsidian replacement,
- capability registry,
- secret store.

## Next

Add capability:

```text
knowledge.source_briefing
```

Then test providers behind it.
