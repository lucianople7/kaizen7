# Ecosystem Master Direction

## Core Thesis

KAIZEN7 is the capability kernel. Flowmatik is the creative operating studio. Kaizen is the human trust interface. THE FOCUX is the premium performance brand. THE FOCUX OS is the educational IP.

The shared path is:

```text
content -> trust -> founding list -> dossiers -> curated selection -> own product
```

## Operating Rule

Improve each project individually, connect through small contracts, verify before publishing, write memory after each useful artifact.

## Current Production Spine

```text
THE FOCUX dossier
-> Flowmatik content pack
-> Kaizen script
-> Remotion video factory
-> KAIZEN7 verification
-> Obsidian memory
```

## Next Build

Create the CLI for a new Flowmatik video from a pasted script.

Required output:

- project JSON,
- generated Remotion data,
- local audio,
- render plan,
- MP4,
- verification,
- memory writeback.

## Video Pipeline Pattern

The useful pattern from the unverified LeronX Engine attachment is:

```text
script -> scenes -> assets -> voice -> render -> subtitles -> post -> export
```

Adopt the pattern, not the dependency, until the repo and license are confirmed from primary sources.
