# KAIZEN7 - 2026-06-28

## Cambio

Se incorporo inteligencia externa curada desde GitHub y Hugging Face sin instalar dependencias:

- `remote_worker` como adaptador de ejecucion remota acotada.
- `external-intelligence` como registro de patrones OpenHands, SWE-agent, aider, AutoGen, bge-m3, reranker, Qwen embeddings y SWE-bench.

## Decision

KAIZEN7 sigue siendo el core. Los agentes remotos son workers reemplazables.

```text
KAIZEN7 decide -> worker ejecuta -> KAIZEN7 verifica -> humano aprueba -> memoria registra
```

## Verificacion

- `npm.cmd run check`: pasa.
- `npm.cmd run k7:ready`: ready, 70 checks, 0 blockers.

## Siguiente accion

Exponer `externalIntelligence` y `remoteWorker` en el Project Activation Cockpit.

## OpenHands

Se anadio `k7:openhands` como adaptador especifico:

```powershell
npm.cmd run k7:openhands -- "use OpenHands to improve KAIZEN7 safely"
```

Tambien queda disponible como API:

```http
POST /api/k7/openhands
```

Decision: OpenHands gana fuerza como worker/control center, pero KAIZEN7 conserva el core. El paquete exige sandbox preferente, paths permitidos, acciones prohibidas, verificacion y retorno por diff.

Verificacion adicional:

- `npm.cmd run check`: pasa.
- `npm.cmd run k7:ready`: ready, 73 checks, 0 blockers.

## Toolchain Router + Eval Firewall

Se anadio `k7:toolchain`:

```powershell
npm.cmd run k7:toolchain -- "use OpenHands to improve KAIZEN7 with tests"
```

API:

```http
POST /api/k7/toolchain
POST /api/k7/toolchain/evaluate
```

Decision: KAIZEN7 pasa de listar adaptadores a seleccionar una cadena minima de herramientas/workers y bloquear resultados sin evidencia.

## Activation Cockpit

Se anadio `k7:cockpit`:

```powershell
npm.cmd run k7:cockpit
```

API:

```http
POST /api/k7/cockpit
```

Decision: KAIZEN7 debe empezar preguntando el objetivo, pedir contexto minimo y construir solo la toolchain necesaria. No megatool, no lectura masiva, no tokens quemados sin motivo.
