# PR #3 CI Gate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give KAIZEN7 pull requests an automatic, reproducible merge gate that runs the repository's existing verification commands.

**Architecture:** Add one GitHub Actions workflow on the existing PR branch. It uses the minimum supported Node.js version, installs exactly from `package-lock.json`, runs the complete check suite, and finishes with the public KAIZEN7 doctor command.

**Tech Stack:** GitHub Actions, Node.js 20, npm.

## Global Constraints

- Use only GitHub-hosted, free repository automation.
- Grant the workflow read-only repository permissions.
- Do not add dependencies or alter KAIZEN7 runtime behavior.
- Keep the workflow portable and deterministic through `npm ci`.

---

### Task 1: Add the pull-request verification gate

**Files:**
- Create: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `package-lock.json`, the `check` npm script, and `lib/k7-cli.js`.
- Produces: the GitHub Actions `KAIZEN7 verification` status check.

- [ ] **Step 1: Confirm the missing gate**

Run: `test ! -e .github/workflows/ci.yml`
Expected: exit code 0.

- [ ] **Step 2: Create the workflow**

Create `.github/workflows/ci.yml` with pull-request and `main` push triggers, read-only permissions, Node.js 20 with npm caching, `npm ci`, `npm run check`, and `node lib/k7-cli.js doctor`.

- [ ] **Step 3: Validate the workflow contract**

Run: `node -e "const fs=require('node:fs');const y=fs.readFileSync('.github/workflows/ci.yml','utf8');for(const value of ['pull_request:','contents: read','node-version: 20','npm ci','npm run check','node lib/k7-cli.js doctor'])if(!y.includes(value))throw new Error('missing '+value)"`
Expected: exit code 0.

- [ ] **Step 4: Re-run repository verification**

Run: `npm run check && node lib/k7-cli.js doctor`
Expected: all tests pass and doctor reports no blocker.

- [ ] **Step 5: Commit and publish to the existing PR branch**

```bash
git add .github/workflows/ci.yml docs/superpowers/plans/2026-07-19-pr3-ci-gate.md
git commit -m "ci: add KAIZEN7 verification gate"
git push origin feature/work-kaizen7-preflight
```

Expected: PR #3 receives a new workflow run.
